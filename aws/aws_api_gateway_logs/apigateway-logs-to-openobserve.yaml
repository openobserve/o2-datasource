AWSTemplateFormatVersion: '2010-09-09'
Description: 'API Gateway Access Logs to OpenObserve via Kinesis Data Streams and Firehose'

Parameters:
  OpenObserveEndpoint:
    Type: String
    Description: 'OpenObserve HTTP endpoint URL (e.g., https://api.openobserve.ai/api/your-org/default/_kinesis)'

  OpenObserveAccessKey:
    Type: String
    NoEcho: true
    Description: 'OpenObserve access key for authentication (base64 encoded user:password)'

  StreamName:
    Type: String
    Default: 'apigateway-logs-stream'
    Description: 'Name for the OpenObserve stream'

  ApiGatewayId:
    Type: String
    AllowedPattern: '^[A-Za-z0-9]+(,[A-Za-z0-9]+){0,4}$'
    ConstraintDescription: 'Between 1 and 5 API Gateway IDs, comma separated, no spaces'
    Description: 'REST API Gateway ID. Up to 5 may be given, comma separated with no spaces (e.g., abc123,def456). Each entry is paired by position with StageName.'

  StageName:
    Type: String
    AllowedPattern: '^[A-Za-z0-9_-]+(,[A-Za-z0-9_-]+){0,4}$'
    ConstraintDescription: 'Between 1 and 5 stage names, comma separated, no spaces, in the same order as ApiGatewayId'
    Description: 'API Gateway Stage name (e.g., prod, dev, staging). Give one stage per API Gateway ID, comma separated with no spaces and in the same order (e.g., prod,dev).'

  ShardCount:
    Type: Number
    Default: 1
    MinValue: 1
    MaxValue: 10
    Description: 'Number of shards for Kinesis Data Stream'

  BackupS3BucketName:
    Type: String
    Default: ''
    Description: 'OPTIONAL — name of an EXISTING S3 bucket for Firehose failed-records backup. Leave blank to have CFN create a fresh bucket (auto-generated unique name, AES256 encryption, 30-day expiration, all public access blocked). Provide a name to reuse an existing bucket you already manage.'

  FilterPattern:
    Type: String
    Default: ''
    Description: 'CloudWatch Logs filter pattern (leave empty to stream all logs)'

  EnableAccessLogging:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: 'Whether to enable access logging for the API Gateway stage (if not already enabled)'

Resources:
  # S3 Bucket for Failed Records
  BackupS3Bucket:
    Type: AWS::S3::Bucket
    Condition: CreateBackupBucket
    Properties:
      # No BucketName — CFN auto-generates, guaranteeing uniqueness.
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldBackups
            Status: Enabled
            ExpirationInDays: 30
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  # CloudWatch Log Group for API Gateway Access Logs
  ApiGatewayLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub
        - '/aws/apigateway/${Api}/${Stage}'
        - Api: !Select [0, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]]
          Stage: !Select [0, !Split [',', !Join [',', [!Ref StageName, '', '', '', '', '']]]]
      RetentionInDays: 7

  # IAM Role for API Gateway to write to CloudWatch Logs
  ApiGatewayLogsRole:
    Type: AWS::IAM::Role
    Condition: ShouldEnableLogging
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: apigateway.amazonaws.com
            Action: 'sts:AssumeRole'
      ManagedPolicyArns:
        - 'arn:aws:iam::aws:policy/service-role/AmazonAPIGatewayPushToCloudWatchLogs'

  # Kinesis Data Stream for API Gateway Logs
  ApiGatewayLogStream:
    Type: AWS::Kinesis::Stream
    Properties:
      Name: !Sub '${AWS::StackName}-apigateway-logs'
      ShardCount: !Ref ShardCount
      RetentionPeriodHours: 24
      StreamModeDetails:
        StreamMode: PROVISIONED

  # IAM Role for Lambda Transformation Function
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: 'sts:AssumeRole'
      ManagedPolicyArns:
        - 'arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole'
      Policies:
        - PolicyName: KinesisReadWrite
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - 'kinesis:GetRecords'
                  - 'kinesis:GetShardIterator'
                  - 'kinesis:DescribeStream'
                  - 'kinesis:ListStreams'
                  - 'kinesis:PutRecord'
                  - 'kinesis:PutRecords'
                Resource: !GetAtt ApiGatewayLogStream.Arn

  # Lambda Function for API Gateway Log Transformation
  LogTransformFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub '${AWS::StackName}-log-transformer'
      Runtime: python3.11
      Handler: index.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 60
      MemorySize: 256
      Code:
        ZipFile: |
          import base64
          import json
          import gzip
          import re
          from datetime import datetime

          def parse_api_gateway_log(message):
              """Parse API Gateway access log message into structured fields"""
              try:
                  # Try to parse as JSON first (if using JSON log format)
                  if message.strip().startswith('{'):
                      return json.loads(message)

                  # Parse custom log format: RequestId: xxx, SourceIP: xxx, Method: xxx, etc.
                  log_data = {}

                  # Extract key-value pairs
                  patterns = {
                      'requestId': r'RequestId:\s*([^,]+)',
                      'sourceIp': r'SourceIP:\s*([^,]+)',
                      'method': r'Method:\s*([^,]+)',
                      'resourcePath': r'ResourcePath:\s*([^,]+)',
                      'statusCode': r'StatusCode:\s*(\d+)',
                      'responseLength': r'ResponseLength:\s*(\d+)',
                      'requestTime': r'RequestTime:\s*([^,]+)',
                      'integrationLatency': r'IntegrationLatency:\s*(\d+)',
                      'latency': r'Latency:\s*(\d+)',
                      'userAgent': r'UserAgent:\s*([^,]+)',
                      'protocol': r'Protocol:\s*([^,]+)',
                  }

                  for field, pattern in patterns.items():
                      match = re.search(pattern, message)
                      if match:
                          value = match.group(1).strip()
                          # Convert numeric fields
                          if field in ['statusCode', 'responseLength', 'integrationLatency', 'latency']:
                              try:
                                  log_data[field] = int(value)
                              except ValueError:
                                  log_data[field] = value
                          else:
                              log_data[field] = value

                  # If no fields extracted, return raw message
                  if not log_data:
                      log_data = {'raw_message': message}

                  return log_data

              except Exception as e:
                  # Return raw message if parsing fails
                  return {'raw_message': message, 'parse_error': str(e)}

          def lambda_handler(event, context):
              output = []

              for record in event['records']:
                  # Decode the data
                  payload = base64.b64decode(record['data'])

                  try:
                      # Decompress the gzip data from CloudWatch Logs
                      decompressed = gzip.decompress(payload)
                      log_data = json.loads(decompressed)

                      # CloudWatch Logs format
                      log_group = log_data.get('logGroup', '')
                      log_stream = log_data.get('logStream', '')
                      log_events = log_data.get('logEvents', [])

                      # Transform each log event
                      transformed_logs = []
                      for event in log_events:
                          message = event.get('message', '')

                          # Parse API Gateway access log
                          parsed_log = parse_api_gateway_log(message)

                          # Build transformed log entry
                          transformed = {
                              'timestamp': event.get('timestamp', int(datetime.utcnow().timestamp() * 1000)),
                              'logGroup': log_group,
                              'logStream': log_stream,
                              'id': event.get('id', ''),
                          }

                          # Merge parsed fields
                          transformed.update(parsed_log)

                          # Add status code category
                          if 'statusCode' in parsed_log:
                              status = parsed_log['statusCode']
                              if isinstance(status, int):
                                  if status < 300:
                                      transformed['statusCategory'] = 'Success'
                                  elif status < 400:
                                      transformed['statusCategory'] = 'Redirect'
                                  elif status < 500:
                                      transformed['statusCategory'] = 'ClientError'
                                  else:
                                      transformed['statusCategory'] = 'ServerError'

                          # Calculate performance indicators
                          if 'latency' in parsed_log and isinstance(parsed_log['latency'], int):
                              latency = parsed_log['latency']
                              if latency > 3000:
                                  transformed['performanceIssue'] = 'Slow'
                              elif latency > 1000:
                                  transformed['performanceIssue'] = 'Warning'
                              else:
                                  transformed['performanceIssue'] = 'Normal'

                          transformed_logs.append(transformed)

                      # Create output with all log events
                      output_data = '\n'.join([json.dumps(log) for log in transformed_logs]) + '\n'
                      output_record = {
                          'recordId': record['recordId'],
                          'result': 'Ok',
                          'data': base64.b64encode(output_data.encode('utf-8')).decode('utf-8')
                      }
                  except Exception as e:
                      print(f"Error processing record: {str(e)}")
                      # Return original data if transformation fails
                      output_record = {
                          'recordId': record['recordId'],
                          'result': 'ProcessingFailed',
                          'data': record['data']
                      }

                  output.append(output_record)

              return {'records': output}

  # IAM Role for Kinesis Firehose
  FirehoseDeliveryRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: firehose.amazonaws.com
            Action: 'sts:AssumeRole'
      Policies:
        - PolicyName: FirehoseDeliveryPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - 's3:AbortMultipartUpload'
                  - 's3:GetBucketLocation'
                  - 's3:GetObject'
                  - 's3:ListBucket'
                  - 's3:ListBucketMultipartUploads'
                  - 's3:PutObject'
                Resource:
                  - !If [CreateBackupBucket, !GetAtt BackupS3Bucket.Arn, !Sub 'arn:aws:s3:::${BackupS3BucketName}']
                  - !If [CreateBackupBucket, !Sub '${BackupS3Bucket.Arn}/*', !Sub 'arn:aws:s3:::${BackupS3BucketName}/*']
              - Effect: Allow
                Action:
                  - 'kinesis:DescribeStream'
                  - 'kinesis:GetShardIterator'
                  - 'kinesis:GetRecords'
                  - 'kinesis:ListShards'
                Resource: !GetAtt ApiGatewayLogStream.Arn
              - Effect: Allow
                Action:
                  - 'lambda:InvokeFunction'
                  - 'lambda:GetFunctionConfiguration'
                Resource: !GetAtt LogTransformFunction.Arn

  # Kinesis Data Firehose to OpenObserve
  OpenObserveFirehose:
    Type: AWS::KinesisFirehose::DeliveryStream
    DependsOn:
      - FirehoseDeliveryRole
    Properties:
      DeliveryStreamName: !Sub '${AWS::StackName}-to-openobserve'
      DeliveryStreamType: KinesisStreamAsSource
      KinesisStreamSourceConfiguration:
        KinesisStreamARN: !GetAtt ApiGatewayLogStream.Arn
        RoleARN: !GetAtt FirehoseDeliveryRole.Arn
      HttpEndpointDestinationConfiguration:
        EndpointConfiguration:
          Url: !Ref OpenObserveEndpoint
          AccessKey: !Ref OpenObserveAccessKey
          Name: OpenObserve
        RequestConfiguration:
          ContentEncoding: GZIP
          CommonAttributes:
            - AttributeName: stream-name
              AttributeValue: !Ref StreamName
            - AttributeName: api-id
              AttributeValue: !Ref ApiGatewayId
            - AttributeName: stage
              AttributeValue: !Ref StageName
        BufferingHints:
          SizeInMBs: 1
          IntervalInSeconds: 60
        RetryOptions:
          DurationInSeconds: 300
        S3BackupMode: FailedDataOnly
        S3Configuration:
          BucketARN: !If [CreateBackupBucket, !GetAtt BackupS3Bucket.Arn, !Sub 'arn:aws:s3:::${BackupS3BucketName}']
          RoleARN: !GetAtt FirehoseDeliveryRole.Arn
          CompressionFormat: GZIP
          BufferingHints:
            SizeInMBs: 5
            IntervalInSeconds: 300
          Prefix: 'failed-logs/'
          ErrorOutputPrefix: 'error-logs/'
        ProcessingConfiguration:
          Enabled: true
          Processors:
            - Type: Lambda
              Parameters:
                - ParameterName: LambdaArn
                  ParameterValue: !GetAtt LogTransformFunction.Arn
                - ParameterName: RoleArn
                  ParameterValue: !GetAtt FirehoseDeliveryRole.Arn

  # IAM Role for CloudWatch Logs to write to Kinesis
  CloudWatchLogsRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: logs.amazonaws.com
            Action: 'sts:AssumeRole'
      Policies:
        - PolicyName: CloudWatchLogsKinesisWrite
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - 'kinesis:PutRecord'
                  - 'kinesis:PutRecords'
                Resource: !GetAtt ApiGatewayLogStream.Arn

  # CloudWatch Logs Subscription Filter
  LogsSubscriptionFilter:
    Type: AWS::Logs::SubscriptionFilter
    DependsOn:
      - ApiGatewayLogStream
      - CloudWatchLogsRole
      - ApiGatewayLogGroup
    Properties:
      LogGroupName: !Ref ApiGatewayLogGroup
      FilterPattern: !Ref FilterPattern
      DestinationArn: !GetAtt ApiGatewayLogStream.Arn
      RoleArn: !GetAtt CloudWatchLogsRole.Arn

  ApiGatewayLogGroup1:
    Type: AWS::Logs::LogGroup
    Condition: HasApi1
    Properties:
      LogGroupName: !Sub
        - '/aws/apigateway/${Api}/${Stage}'
        - Api: !Select [1, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]]
          Stage: !Select [1, !Split [',', !Join [',', [!Ref StageName, '', '', '', '', '']]]]
      RetentionInDays: 7

  LogsSubscriptionFilter1:
    Type: AWS::Logs::SubscriptionFilter
    Condition: HasApi1
    DependsOn:
      - ApiGatewayLogStream
      - CloudWatchLogsRole
      - ApiGatewayLogGroup1
    Properties:
      LogGroupName: !Ref ApiGatewayLogGroup1
      FilterPattern: !Ref FilterPattern
      DestinationArn: !GetAtt ApiGatewayLogStream.Arn
      RoleArn: !GetAtt CloudWatchLogsRole.Arn

  ApiGatewayLogGroup2:
    Type: AWS::Logs::LogGroup
    Condition: HasApi2
    Properties:
      LogGroupName: !Sub
        - '/aws/apigateway/${Api}/${Stage}'
        - Api: !Select [2, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]]
          Stage: !Select [2, !Split [',', !Join [',', [!Ref StageName, '', '', '', '', '']]]]
      RetentionInDays: 7

  LogsSubscriptionFilter2:
    Type: AWS::Logs::SubscriptionFilter
    Condition: HasApi2
    DependsOn:
      - ApiGatewayLogStream
      - CloudWatchLogsRole
      - ApiGatewayLogGroup2
    Properties:
      LogGroupName: !Ref ApiGatewayLogGroup2
      FilterPattern: !Ref FilterPattern
      DestinationArn: !GetAtt ApiGatewayLogStream.Arn
      RoleArn: !GetAtt CloudWatchLogsRole.Arn

  ApiGatewayLogGroup3:
    Type: AWS::Logs::LogGroup
    Condition: HasApi3
    Properties:
      LogGroupName: !Sub
        - '/aws/apigateway/${Api}/${Stage}'
        - Api: !Select [3, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]]
          Stage: !Select [3, !Split [',', !Join [',', [!Ref StageName, '', '', '', '', '']]]]
      RetentionInDays: 7

  LogsSubscriptionFilter3:
    Type: AWS::Logs::SubscriptionFilter
    Condition: HasApi3
    DependsOn:
      - ApiGatewayLogStream
      - CloudWatchLogsRole
      - ApiGatewayLogGroup3
    Properties:
      LogGroupName: !Ref ApiGatewayLogGroup3
      FilterPattern: !Ref FilterPattern
      DestinationArn: !GetAtt ApiGatewayLogStream.Arn
      RoleArn: !GetAtt CloudWatchLogsRole.Arn

  ApiGatewayLogGroup4:
    Type: AWS::Logs::LogGroup
    Condition: HasApi4
    Properties:
      LogGroupName: !Sub
        - '/aws/apigateway/${Api}/${Stage}'
        - Api: !Select [4, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]]
          Stage: !Select [4, !Split [',', !Join [',', [!Ref StageName, '', '', '', '', '']]]]
      RetentionInDays: 7

  LogsSubscriptionFilter4:
    Type: AWS::Logs::SubscriptionFilter
    Condition: HasApi4
    DependsOn:
      - ApiGatewayLogStream
      - CloudWatchLogsRole
      - ApiGatewayLogGroup4
    Properties:
      LogGroupName: !Ref ApiGatewayLogGroup4
      FilterPattern: !Ref FilterPattern
      DestinationArn: !GetAtt ApiGatewayLogStream.Arn
      RoleArn: !GetAtt CloudWatchLogsRole.Arn

Conditions:
  CreateBackupBucket: !Equals [!Ref BackupS3BucketName, '']
  ShouldEnableLogging: !Equals [!Ref EnableAccessLogging, 'true']

  # One log group + subscription filter per comma separated API/stage pair.
  # Slot 0 is always created; the rest switch on as more pairs are supplied.
  HasApi1: !Not [!Equals [!Select [1, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]], '']]
  HasApi2: !Not [!Equals [!Select [2, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]], '']]
  HasApi3: !Not [!Equals [!Select [3, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]], '']]
  HasApi4: !Not [!Equals [!Select [4, !Split [',', !Join [',', [!Ref ApiGatewayId, '', '', '', '', '']]]], '']]

Outputs:
  ApiGatewayId:
    Description: 'API Gateway ID(s) being streamed'
    Value: !Ref ApiGatewayId

  StageName:
    Description: 'API Gateway Stage Name(s), paired by position with ApiGatewayId'
    Value: !Ref StageName

  LogGroupName:
    Description: 'CloudWatch Log Group Name for the first API/stage pair'
    Value: !Ref ApiGatewayLogGroup

  KinesisStreamName:
    Description: 'Kinesis Data Stream Name'
    Value: !Ref ApiGatewayLogStream

  KinesisStreamArn:
    Description: 'Kinesis Data Stream ARN'
    Value: !GetAtt ApiGatewayLogStream.Arn

  FirehoseDeliveryStreamName:
    Description: 'Firehose Delivery Stream Name'
    Value: !Ref OpenObserveFirehose

  BackupS3BucketName:
    Description: 'S3 Bucket for Failed Records'
    Value: !If [CreateBackupBucket, !Ref BackupS3Bucket, !Ref BackupS3BucketName]

  SubscriptionFilterName:
    Description: 'CloudWatch Logs Subscription Filter Name for the first API/stage pair'
    Value: !Ref LogsSubscriptionFilter

  NextSteps:
    Description: 'Next Steps'
    Value: !Sub |
      1. Enable access logging in the API Gateway console for every API/stage pair
         given in ApiGatewayId (${ApiGatewayId}) and StageName (${StageName})
      2. Point each stage at its own log group, named /aws/apigateway/<api-id>/<stage>.
         The first pair's log group ARN is: ${ApiGatewayLogGroup.Arn}
      3. Access logs will be streamed to OpenObserve automatically
